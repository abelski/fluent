# Feature / Change Log

Sequential log of features and notable changes, so change order is legible across sessions.
Append one entry per feature/change (highest number = most recent). Reuse the number when
naming the corresponding plan file, e.g. `plans/improvements/active/plan_<N>_<slug>.md`.

| # | Date | Description |
| --- | --- | --- |
| 1 | 2026-08-14 | Mobile usability fixes: nested `<button>`-in-`<button>` accordion headers converted to `role="button"` divs (lists/phrases/grammar), icon-only touch targets bumped to 44×44px, `ProgressStatCard` hero stacks on mobile, grammar subcategory stat text no longer wraps, articles category pills scroll instead of wrapping into a stadium shape, star-level tooltip now flashes on tap. |
| 2 | 2026-08-18 | Fix #156: grammar instrumental of *duktė* graded against a wrong ending (`eria`) but displayed a different wrong "correct answer" (`dukteri`); both were also linguistically wrong (`dukterimi`). Fixed `words.txt` (`ses`/`dukt` case_index 5), production row 204, and one Cyrillic/Czech-glyph-corrupted row (id 73, `agurků`→`agurkų`) found via a full `grammar_sentence` invariant audit. Added a `stem(display)+answer_ending == full_word` invariant guard: filters bad rows out of served sentence tasks (`_sentence_invariant_holds` in `grammar_service.py`) and rejects violating writes with a 400 in the admin create/update endpoints. See `documentation/grammar-sentence-data-integrity.md`. |
| 3 | 2026-08-18 | Fix #157: weekly leaderboard reward job scored `DATE_TRUNC('week', NOW())` at its Monday 10:00 UTC run time, so it granted Premium based on ~10 hours of the *new* week instead of the 7 days that just ended. Added `scheduler.previous_week_bounds()` / `leaderboard_service.current_week_bounds()` (pure, injectable-`now` helpers) so the reward job, its manual-trigger endpoint, and the admin top-5 preview all target the last *completed* week, while the user-facing leaderboard widget keeps showing the in-progress week (different requirements — see `documentation/reward-vs-leaderboard-week.md`). Also: de-duplicated the manual `POST /api/admin/leaderboard-rewards/generate` path onto the same `generate_weekly_reward_messages()` the cron job uses; anchored the reward-dedup window to the rewarded week instead of a rolling 6 days; unified reward/leaderboard/admin-preview scoring onto one shared 4-source SQL builder (`leaderboard_service.build_leaderboard_score_joins`); fixed "this week" email copy that would otherwise contradict a previous-week reward; added `misfire_grace_time`/`coalesce` to the cron job so a missed run can now be safely recovered. |
| 4 | 2026-08-20 | Fix #158: the instrumental (Įnagininkas) rule card described only declensions I–III, while the exercise pool contains IV (*sūnus*, *profesorius*) and V (*sesuo*, *duktė*, *vanduo*) nouns. Because the learner types the **stem-relative** ending while the card was written in **nominative** terms, the card did not merely omit those forms — it actively mispredicted them (`-is/-ys→-iu` implies *profesoriu*, but the graded answer is *profesoriumi*). Rewrote `grammar_case_rule` id 8 (`transform`, `endings_sg`, `endings_pl`) to state the IV/V mappings; fixed `grammar_sentence` id 126, which stored *seseria* where `words.txt` gives *seserimi* (issue #156 fixed *duktė* and `words.txt` but missed this row — it passes the `stem+ending == full_word` invariant, so #156's filter never caught it); corrected the *daiktavardžiai-linksniavimas* article's `akmen-imi`/`ruden-imi` cells to `akmen-iu`/`ruden-iu`, which the new card text would otherwise contradict. Added a new invariant — *every `answer_ending` reachable in a case's live sentences must be derivable from that case's rule card* — enforced by `backend/scripts/audit_case_rule_coverage.py` and the live-data half of `frontend/tests/issue-158-instrumental-iv-v-declension.spec.ts`. **Scope deliberately narrowed to the reported case (5):** the same I–III-only gap exists in cases 2, 3, 4, 6, 7, 8, 9 and 13 and is tracked as known debt via `GUARDED_CASES`/`DEFERRED_CASES` in both the script and the spec, with verified replacement strings kept in the plan file's Deferred section. Note case 7 is a coupled pair — sentence 198 (*dukte*) is wrong, but rule 12's `-ė→-e` predicts exactly *dukte*, so fixing the row alone would recreate this bug there. See `documentation/grammar-sentence-data-integrity.md`. |
| 5 | 2026-08-20 | Review flow rework: maturity-aware stage graph + interleaved scheduling. A **mature** word (`status=known` and `sm2_reps >= MATURE_WORD_REPS`, decided server-side and sent as `mature`) now opens on the typing card with no answer-revealing flashcard; a miss or the new «Забыл» button drops it into the full learning flow (CARD → SELECT → ASSEMBLE → TYPE). «Легко» → type, and a typing miss demotes to the difficult path (SELECT → ASSEMBLE → TYPE) — which now runs for **every** entry type, because `buildAssemblyTiles` (`frontend/lib/assembleTiles.ts`) picks whole-word, syllable or letter tiles instead of `isSingleWordEntry` skipping phrases, multi-form entries and one-syllable words entirely. A mistake anywhere in a chain costs 2× assemble + 2× type, once per word per session (1× + 1× in quick mode). Cards are no longer spliced in as a contiguous block: `scheduleCards` (`frontend/lib/scheduleCards.ts`) interleaves them so no two cards of one word sit next to each other and no three consecutive cards share an exercise bucket — the `'3s'` near-miss drill is the one deliberate exception, keeping its near-the-front placement. Two words with an identical translation can no longer meet: `_dedupe_by_translation` runs in all six session-building endpoints with a `limit × 3` over-fetch so a dropped twin is backfilled, `pickDistractors`/`buildOptions2r` also de-duplicate on the *displayed* translation, and the collision key deliberately **keeps** parentheses so issue #152's qualifiers stay distinguishable. Audit: `backend/scripts/audit_duplicate_translations.py` (52 ru groups / 133 words, 20 same-list). See `documentation/review-flow-stage-graph.md`. |
| 6 | 2026-08-22 | Fix #159: the Dative (Naudininkas) and Vocative (Šauksmininkas) rule cards described only declensions I–III, so the reported gap — no rule for u-stem (declension IV) nouns like *sūnus* — actively mispredicted the answer rather than merely omitting it, same bug class as #158. This closes two of #158's own deferred follow-up cases: rewrote `grammar_case_rule` id 4 (case 3, Dative) to state `sūnus→sūnui`, `profesorius→profesoriui` and the V-declension mappings; rewrote id 12 (case 7, Vocative) to state `sūnus→sūnau`, `profesorius→profesoriau`, `duktė→dukterie`. Case 7 was a coupled pair: sentence row 198 (`Ačiū, dukt___!`) graded `dukte`, which agreed with the old (wrong) rule by coincidence — `words.txt` gives the true vocative of *duktė* as `dukterie`, so row 198 (`answer_ending`/`full_word`) and rule 12 were fixed together in one change, not independently. Widened `GUARDED_CASES` from `{5}` to `{3, 5, 7}` (and `DEFERRED_CASES` correspondingly) in both `backend/scripts/audit_case_rule_coverage.py` and `frontend/tests/issue-158-instrumental-iv-v-declension.spec.ts`, so the existing generalized "every gradeable ending is derivable from its rule card" guard now also covers cases 3 and 7. Cases 2, 4, 6, 8, 9 and 13 remain tracked debt. See `documentation/grammar-sentence-data-integrity.md`. |
| 7 | 2026-08-22 | Follow-up to #159, found via user feedback immediately after #6 shipped: the new Dative/Vocative rule-card examples (`sūnus`, `profesorius`, `duktė`) were themselves graded answers in those same two lessons — `duktė→dukterie` was the literal answer to the sentence the fix had just corrected, so a student could read it straight off the card. New invariant — a rule card's example words must not equal an answer in its own case's exercise pool — found the same leak, pre-existing and unfixed, in 9 other cases (2, 5, 6, 8, 9, 10, 11, 12, 13). Fixed only the two rows touched by #159 (swapped to `turgus`/`vaisius`/`vanduo`/`namas`/`maišelis`/`knyga`/`gatvė`, none of which appear in either lesson's exercises, verified against `words.txt`); the other 9 cases are tracked as known debt, not yet actioned. See "Second invariant" in `documentation/grammar-sentence-data-integrity.md`. |
| 8 | 2026-08-22 | At `practice` level, noun-case `sentence` exercises now require typing the whole inflected word instead of just the case ending, matching how `declension`/`verb_conjugation` tasks already work — `basic`/`advanced` stay unchanged (stem pre-printed, ending-only). `_generate_sentence_tasks` (`backend/grammar_service.py`) strips the stem span out of `display` at practice level and grades against `stem + answer_ending`; `full_answer` (shown on a wrong guess) is untouched in both branches. Hoisted the stem-capture regex into a module-level `_STEM_BLANK_RE`, reused by `_extract_stem` and the new practice-level strip, so capture and strip can never target a different span of `display`. Cases 17–19's non-inflecting numeral prefix (e.g. `"dvidešimt"`) is outside the captured stem, so it's untouched by the strip and stays visible as plain sentence text — practice level there still grades only the inflecting word (`"pirmu"`), never the longer `row.full_word` (`"dvidešimt pirmu"`). This is a pure derivation from existing columns, not a new invariant — see "Practice-level full-word answers are derived" in `documentation/grammar-sentence-data-integrity.md`. No frontend/DB/admin changes needed: `InlineSentenceInput`/`checkAnswer()` in `GrammarTaskRunner.tsx` were already fully generic over served string content/length. New tests: `backend/tests/test_grammar_practice_full_word.py`, `frontend/tests/grammar-practice-full-word-answer.spec.ts`. Also fixed 12 pre-existing, unrelated `tsc --noEmit` errors across 5 test files blocking the plan's Definition of Done. **Follow-up found via a full-suite run (not caught by the plan's curated regression list):** the same practice-level `task.answer` shape change broke `frontend/tests/issue-158-instrumental-iv-v-declension.spec.ts`'s live rule-card-coverage audit the same way it broke issue-159's (see `documentation/grammar-sentence-data-integrity.md`); both specs now exclude `level === 'practice'` from that specific check. |
| 9 | 2026-08-25 | Grammar content-quality pass across all three public grammar programs, plus removal of the free-tier quota banner. Every noun-case rule card (case_index 2–13) was rewritten to the vocative card's format — `suffix→suffix (word→word)` grouped by declension — so that (a) every ending the lesson grades is derivable from the card, closing #158's six deferred cases (2, 4, 6, 8, 9, 13) and moving `GUARDED_CASES` to `range(2, 14)` with `DEFERRED_CASES` now empty, in `backend/scripts/audit_case_rule_coverage.py` and `frontend/tests/issue-158-instrumental-iv-v-declension.spec.ts`; (b) no example word is itself a graded answer in that same lesson, closing entry #7's 9-case debt table — where the only possible example *is* an answer (`sesuo`/`duktė` are the whole V feminine class) the card now states `sesuo/duktė→-erį`, naming the source but not the answer; and (c) every example makes real-world sense for what the case means, extending #162's locative rule to the other cases (a dative example must be a plausible recipient: `turgus→turgui` → `muziejus→muziejui`). New committed check `backend/scripts/audit_rule_card_examples.py` enforces (b) — strictly for noun cases 2–13, informationally for the numeral cases 15–20, where the closed-class paradigm *is* the rule and cannot be taught without naming the forms. The six numbers cards (15–20) were rewritten to state their actual patterns (`-i→-ios`, `-i→-is`, `-as→-u`, compound numerals inflect only the last word) with `backend/scripts/seed_numbers_grammar.py` kept literally in sync, and the six `tense_hints` in `backend/data/grammar/verb_lessons.json` were rewritten to fix the same coverage bug there (present-tense III conjugation `skaitau`/`skaitai` was mispredicted, and reflexive `-tis` endings — ~12% of the verb pool — were entirely absent). Sentence-level fixes: 13 case-17 rows spelling `troleibusu` with a Cyrillic `у`, `Aštuonios sesers`, `turi aštuonis pusbrolių`, `Vaikai mokosi bažnyčiose`, `Stalui yra keturios kojos` and five wrong Russian glosses; the linked `daiktavardžiai-linksniavimas` article's `paukšč-iuje` locative cell (which the new case-6 card would contradict) fixed in both bodies. `QuotaBanner` removed from `/dashboard/lists` and `/dashboard/phrases` and the component deleted — quota state, the `/api/me/quota` fetch and `limitReached`/`eligible` gating all stay. See `plans/improvements/active/plan_9_grammar-content-quality.md` and `documentation/grammar-sentence-data-integrity.md`. |
| 10 | 2026-08-26 | Premium/admin users can now take grammar lessons in any order — the sequential "lesson N locked until N-1 scores > 75%" rule is bypassed for them in both lesson families (cases `id < 200`, verbs `id >= 200`), via `_lock_bypassed()` in `backend/routers/grammar.py` reusing the same `user.is_admin or is_premium_active(user)` gate as personal word lists. Closes a pre-existing gap found while implementing it: `is_locked` was **list-endpoint metadata only**, so `GET /grammar/lessons/{id}/tasks` and `/grammar/verb-lessons/{id}/tasks` served locked lessons to anyone asking by id — the disabled button on `/dashboard/grammar` was the whole gate. Both task endpoints now resolve the lock through the annotators and return 403, checked **before** `quota_check_and_increment` so a rejected attempt doesn't burn a daily session. `list_verb_lessons`'s inline lock loop was extracted to `_annotate_verb_lesson_progress()` so the verb task endpoint reuses it instead of reimplementing the rule. Frontend: `startLesson` now checks `r.status` (403 → locked screen, 429 → daily-limit screen, other non-OK → back to the list) — previously the status was ignored and any non-200 fell through to an empty task list, i.e. a permanent spinner; locked cards get a small amber `/pricing` upsell rendered outside the disabled card button so it stays clickable. No new frontend premium fetch: with the bypass server-side, `is_locked === true` already means "authenticated, free, previous lesson unpassed". **Fixed a coupled pre-existing bug found while implementing this:** that tasks fetch sent no `Authorization` header (it never had), so neither the new 403 nor the pre-existing daily-session 429 could actually fire from this page — confirmed with the user, then fixed in the same change by adding the header (matching `fetchLessons`'s existing pattern two lines above it). This also means grammar lessons now count against the free tier's daily quota for the first time from this page, which the endpoint's own docstring already claimed but was silently unreachable. New tests: `backend/tests/test_grammar_premium_lock.py`, `frontend/tests/grammar-premium-lesson-order.spec.ts`. See `documentation/grammar-lesson-lock-and-premium.md` and `plans/improvements/active/plan_10_premium-grammar-lesson-order.md`. |
| 11 | 2026-08-29 | Self-serve Premium: €4.50/month recurring subscription via hosted Stripe Checkout + Customer Portal, replacing the `mailto:` CTA that was `/pricing`'s only path to Premium (previously admin-granted or won as a weekly leaderboard reward). Entitlement machinery is unchanged — `is_premium`/`premium_until` read through `is_premium_active()` — Stripe only keeps `premium_until` pointing at the end of the paid period, so nothing downstream knows Stripe exists. New `backend/stripe_service.py` (every `stripe.*` call, config read at **call** time so a keyless checkout still imports) and `backend/routers/billing.py` (`/billing/config` public, `checkout-session` and `portal-session` behind JWT, `webhook` unauthenticated — the signature *is* the auth). Three design rules the tests pin: the **webhook is the only writer of entitlement** (`success_url` is attacker-controllable, so the success screen only *polls* `/api/me/quota`); handlers write `premium_until` as an **absolute** period end, never `+= 1 month`, which makes Stripe's redeliveries idempotent by construction and is why there is no processed-event table; but clamped with `max()` so a renewal cannot silently delete leaderboard-gifted days. Cancellation revokes nothing — `customer.subscription.deleted` leaves `premium_until` alone and `is_premium_active()` lapses the user on its own, so there is no expiry job. Migration `d5e6f7a8b9c0` adds `stripe_customer_id` (indexed), `stripe_subscription_id`, `subscription_status`; `/api/me/quota` and the admin users list expose the status rather than a new endpoint. `/pricing` becomes stateful with a 4-state CTA (login / contact fallback / upgrade / manage) and a `?checkout=success` flow that waits for the webhook — `activating` → `activated`, or "can take a minute" if the poll budget runs out — keeping its documented heavy-border deviation. Ships **dark**: `/billing/config` reports `enabled:false` until the three `STRIPE_*` vars are set in Render, and the pre-#11 contact CTA and beta banner render until then. Tests: `backend/tests/test_billing_webhook.py`, `backend/tests/test_billing_endpoints.py`, `frontend/tests/stripe-checkout-cta.spec.ts`. See `documentation/stripe-subscriptions.md` and `plans/improvements/active/plan_11_stripe-subscription.md` (its Part 0 is the Stripe-dashboard setup checklist — product/price, keys, webhook endpoint, Customer Portal activation, per mode). **Follow-up found after going live:** `subscription_status` was already returned by `/api/admin/users` but never rendered anywhere in the admin panel — added a small badge next to the Premium badge in the Users table (`active`/`past_due`/`canceled`), linking out to the customer in the Stripe dashboard via the also-newly-exposed `stripe_customer_id`. New test: `frontend/tests/admin-subscription-status.spec.ts`. |
| 12 | 2026-08-29 | Fix: static export served with no `Cache-Control` header at all, on every path — found because a visitor loaded `/pricing/` right after the #11 deploy and saw the pre-deploy page (old beta banner, `mailto:` CTA) until a manual hard refresh. Without an explicit header, browsers fall back to heuristic freshness off `Last-Modified`, which can hold a stale HTML page for a while after any deploy. Added `_static_file_response()` in `backend/main.py` (used by both `root()` and `serve_frontend()`, the two places that were calling bare `FileResponse`): `_next/static/*` — Next's content-hashed JS/CSS, safe forever — gets `public, max-age=31536000, immutable`; everything else (HTML pages, whose content changes on every deploy but whose URL doesn't) gets `no-cache`, forcing a cheap ETag-backed revalidation on every load instead of trusting a guess. New test: `backend/tests/test_static_cache_headers.py`. |
| 13 | 2026-08-30 | Telegram notification on Stripe payment events: the webhook (`backend/routers/billing.py`) now pings the admin's existing Telegram chat (`telegram_service.send_telegram`, the same bot as feedback/reports/scheduler) with email, amount + currency, "Fluent Premium" and the Stripe **event id**. Both halves already existed independently — `billing.py` simply never imported `telegram_service`. Notifies on `checkout.session.completed` (new subscriber), `invoice.paid` **only when** `billing_reason == "subscription_cycle"` (a real renewal), `invoice.payment_failed` and `customer.subscription.deleted`; silent on `customer.subscription.updated` and on anything outside `HANDLED_EVENTS`. The `billing_reason` gate matters because Stripe creates and immediately pays the *first* invoice of a new subscription, so one purchase delivers two events — ungated, a signup would send two messages and the second would falsely read "renewed" on day one. `customer.subscription.updated` is silent because it overlaps `invoice.paid` on every renewal and also fires on trial/plan/dunning changes where no payment happened. **Entitlement logic is untouched** — each branch only sets a local `notify_text` and the single send happens after the shared `session.commit()`, so a failed DB write can never produce a "payment succeeded" ping and one delivery yields at most one message. Dispatched via `await asyncio.to_thread(...)` because `send_telegram()` is a blocking `httpx.post(timeout=5)` and `stripe_webhook` is the only `async def` caller — a direct call would stall the event loop for every concurrent request and push the webhook's response toward Stripe's delivery timeout (which triggers retries). Stripe redelivery can duplicate a ping: accepted, not solved, same stance as the missing processed-event table. Test isolation was a real hazard here — `backend/.env` holds real Telegram credentials read at call time, so `backend/conftest.py` now neutralises `send_telegram` twice: a module-level no-op at import time (covers `main.py`'s startup ping, which fires inside the *session*-scoped `client` fixture, i.e. before any function-scoped fixture exists — that ping had in fact been hitting the real Bot API on every test run) plus an **autouse** `_telegram_spy` list-appender fixture protecting every test file, present and future. New tests: section 6 of `backend/tests/test_billing_webhook.py` (7 cases, incl. "one signup ⇒ exactly one message"). Also filled a pre-existing gap in `backend/.env.example`, which was missing all five `STRIPE_*`/`TELEGRAM_*` vars. See `documentation/stripe-subscriptions.md` ("Telegram notifications") and `plans/improvements/active/plan_13_stripe-telegram-notification.md`. |
| 14 | 2026-08-30 | The two remaining non-clickable "Premium" badges now link to `/pricing`: the header's own-status Premium pill (`Header.tsx`, shown next to the avatar once a user already has the plan) and the amber "Premium" badge on a locked row in `/dashboard/practice/[id]`. The header badge sits inside the account-menu toggle `<button>`, so it can't be a nested `<a>`/`Link` — same constraint as the nested-button accordion headers fixed in #1 — so it's a `role="button"` element whose `onClick`/`onKeyDown` (Enter/Space) stop propagation before calling `router.push('/pricing')`, so clicking it no longer also toggles the dropdown; the practice-page badge isn't nested in anything interactive, so it's a plain `Link href="/pricing"`. Both keep their existing test id, visible text and (for the header) hover tooltip unchanged, and reuse their existing emerald/amber color family for the hover state. New test: `frontend/tests/premium-badge-click-to-pricing.spec.ts`. See `plans/improvements/active/plan_13_premium-badge-click-to-pricing.md` (note: filed and numbered `13` before a concurrently-completed plan claimed changelog entry `#13` first — this entry uses `#14` to keep the log's numbers unique; the plan file itself keeps its original `plan_13_...` name). |
| 15 | 2026-08-30 | Fix excessive Neon egress: `GET /api/me/stats` (the dashboard-home/grammar-page stats bar) and `GET /api/me/activity-calendar` fetched every row of a user's `UserWordProgress`/`GrammarLessonResult`/`PracticeExamResult`/`UserPhraseProgress`/`UserCustomPhraseProgress` — no `LIMIT` — just to compute ~9 summary integers and a study streak in Python. `StatsBar.tsx` refetches `/me/stats` on every mount **and** on every browser-tab `visibilitychange`, so for a heavy user (one real account has 922 studied words, 491 mistakes) this shipped thousands of full rows out of Neon on nearly every dashboard page load — the actual cause of the account exhausting Neon's free-tier 5GB/month data-transfer quota (not compute-hours or storage, confirmed from the Neon dashboard). Rewrote both endpoints in `backend/routers/words.py` to compute the same values via SQL: `known`/`learning`/`mistakes`/`due_review`/`phrases_learned`/`phrases_due_review` use a portable `COALESCE(SUM(CASE WHEN ... THEN 1 ELSE 0 END), 0)` pattern (chosen over `func.count().filter()` because the SQL `FILTER` clause isn't uniformly supported, and this repo's tests run against SQLite while production is Postgres); `grammar_lessons_passed` computes best-score-per-lesson via `MAX(score::float / NULLIF(total, 0)) GROUP BY lesson_id`; the study streak fetches only `DISTINCT` calendar dates (`func.date(col, type_=Date)`) per source table instead of every raw row, then keeps the identical backward-walk-from-today loop over that much smaller set. Zero API/response-shape change, so `StatsBar.tsx` and every other caller needed no changes. Verified two ways: `backend/tests/test_stats_query_efficiency.py` seeds edge cases (no progress, a streak-breaking gap, `due_review` exactly on today's boundary, a lesson passed only via its best of two attempts, a stage-1 phrase that must NOT count toward `phrases_learned`) and asserts exact expected output; separately, byte-diffed the live JSON response for a real 773-known-word production account between the original and rewritten code (git-stash swap, same running server) — identical both times. A smaller, lower-severity over-fetch in `backend/grammar_service.py` (fetches a case's entire sentence pool on every lesson-task request, though only 20-35 rows are used) was found during the investigation but deliberately deferred — see `plans/improvements/active/plan_15_stats-endpoint-egress-fix.md`. No infrastructure change; a Render Postgres migration was fully investigated and planned first, then abandoned once this turned out to be a query-efficiency bug rather than a capacity problem. |
| 16 | 2026-08-30 | Pricing nav link + four free→Premium conversion CTAs, closing the gap where `/pricing` had no menu entry point and the end-of-lesson screen said nothing about Premium. `Header.tsx`'s `navLinks` gained a 6th pill, "Тарифы"/"Pricing" (`tr.nav.pricing`), active-highlighted via the same `pathname.startsWith('/pricing')` pattern as the other 5 — joining only the shared pill strip, not the 5-page `PageShell`/`NAV_PAGES` family (`/pricing` keeps its documented heavy-border layout). `QuizSession.tsx`'s "done" screen now shows a support-the-mission upsell card (`data-testid="premium-upsell-card"`/`"premium-upsell-cta"`) for every non-Premium user finishing a study or review session, fetching `/api/me/quota` once on mount (same pattern as `PricingClient.tsx`). `PricingClient.tsx`'s Premium price row gained a "less than a burger or a specialty coffee — per month" caption (`data-testid="price-comparison"`). New `DailyLimitBanner.tsx` renders a small conditional notice on `/dashboard/lists`/`/dashboard/phrases` (reusing each page's existing `quota` state, no new fetch) **only** when a free user has 1 or 0 sessions left today — deliberately not an always-on banner, since `QuotaBanner` was removed from these same two pages in #9 at the user's explicit request for being exactly that. `StatsBar.tsx` gained a one-time milestone nudge: crossing a new streak threshold (`[30,14,7,3]`, highest first) or known-words threshold (`[100,50]`) shows a dismissible card (`data-testid="milestone-nudge"`/`"milestone-nudge-dismiss"`) using the matching motivational headline, deduped per-threshold via `localStorage['fluent_milestone_streak_shown']`/`['fluent_milestone_words_shown']` so it never repeats. The daily-limit and milestone copy reuse **pre-existing, previously-unused** strings (`tr.lists.limitReached`/`getPremium`, `tr.stats.motivations.*`) left over from `QuotaBanner`'s removal and an unwired milestone feature — this change revives dead i18n strings rather than adding new ones for the same purpose. 8 new i18n keys across `nav`/`pricing`/`common`/`lists`/`stats` (RU+EN). New tests: `frontend/tests/pricing-nav-link.spec.ts`, `frontend/tests/premium-upsell-end-of-lesson.spec.ts`, `frontend/tests/daily-limit-banner.spec.ts`, `frontend/tests/milestone-nudge.spec.ts`. No entitlement/Stripe/pricing-amount changes — presentation and marketing copy only. See `plans/improvements/active/plan_16_pricing-conversion-ctas.md`. |
| 17 | 2026-08-31 | Fix: completing a practice exam (`/dashboard/practice`, `POST /practice/tests/{id}/results` → `PracticeExamResult`) silently didn't count toward the dashboard streak (`GET /me/stats`) or the 28-day activity calendar (`GET /me/activity-calendar`) — same bug class as issue #141 (`UserCustomPhraseProgress` missing from the same manually-maintained union), just a different activity table nobody had added to the union list. Added `PracticeExamResult.created_at` (keyed on `PracticeExamResult.user_id`) as a fifth `_distinct_dates(...)` branch in both `get_stats()` and `get_activity_calendar()` in `backend/routers/words.py`, and updated both endpoints' docstrings to mention practice exams alongside words/grammar/phrases so the doc doesn't go stale the way it did for phrases before #141. Also added `test_review_counts_toward_streak` — a **regression test, not a fix** — pinning down that spaced-repetition review (`/dashboard/review`, `GET /review/known` → `POST /words/{id}/progress`) already reached `UserWordProgress.last_seen` (already in the union) and so already counted correctly, so a future refactor of the review endpoints can't silently break that without a test noticing. **Deliberately out of scope, confirmed with the user:** article reads (`/dashboard/articles`) still do not count toward the streak — there is no per-user article progress table or authenticated read endpoint at all, so tracking them would mean designing new infrastructure from scratch, not fixing an existing gap; this remains a known, deferred gap. New tests: `test_practice_exam_counts_toward_streak`, `test_practice_exam_appears_in_activity_calendar`, `test_review_counts_toward_streak` in `backend/tests/test_streak.py`. See `plans/improvements/active/plan_17_practice-exams-count-toward-streak.md`. |
| 18 | 2026-09-01 | Every outgoing user email now soft-upsells non-premium users toward `https://fluent.lt/pricing`, framed around project sustainability (Premium covers hosting/infrastructure and keeps Fluent running) rather than a plain feature list. New `build_premium_upsell(lang, variant)`/`append_premium_upsell(body, is_premium, lang, variant)` in `backend/email_templates.py` (pure string helpers, no DB import, three copy variants — `generic`, `convert`, `inactive`) are called at all four real send sites, each computing `is_premium_active(user)` itself: `reports.py::_notify_reporter` (bilingual report-status email, `lang="both"`), `scheduler.py::generate_inactive_messages` (bakes the `inactive`-variant upsell — which states Premium accounts are exempt from the email's existing, still-unenforced 7-day deletion warning, copy-only, no enforcement job added — into the stored `PreparedMessage.body` at generation time, so both the immediate send and any later admin edit/resend carry it), `scheduler.py::generate_weekly_reward_messages` (`convert` copy for top-3 reward recipients since they just got a free week to convert from, `generic` for rank 4-5 notice recipients, alongside — not replacing — the notice email's pre-existing gamified "reach top 3 for free Premium" pitch), and `admin.py::send_email_to_user` (superadmin ad-hoc send, already gated on `email_consent`). `admin.py::send_prepared_message` needed no change — it already forwards `msg.body` verbatim, so whatever upsell was baked in at generation time rides along automatically. A user for whom `is_premium_active(user)` is `True` never sees the upsell on any of the four paths. Test-harness note: `generate_weekly_reward_messages`'s raw-SQL leaderboard query isn't exercised end-to-end against SQLite anywhere (pre-existing constraint, not introduced here), so its new per-row upsell logic is covered by a focused unit test that replicates the snippet against real `User` objects rather than calling the function itself. New/extended tests: `backend/tests/test_email_templates.py`, `backend/tests/test_reports.py`, `backend/tests/test_scheduler.py`, `backend/tests/test_admin_send_email.py`. See `plans/improvements/active/plan_18_premium-email-upsell.md`. |
| 20 | 2026-09-09 | Verb words now show their three principal forms (infinitive – 3rd-person present – 3rd-person past, e.g. `duoti – duoda – davė`) during study, per user feedback that studying `suprasti` should also surface `supranta`/`suprato` nearby. `Word` gained three nullable columns (`part_of_speech`, `verb_present_3p`, `verb_past_3p`, Alembic `b8c9d0e1f2a3`), resolved by new `backend/verb_lookup.py`: the curated ~365-verb `Verb` table first (hand-verified, stress-mark-folded matching so it actually hits — a literal comparison had missed all but 19 of ~3900 words), then a Wiktionary HTML-scrape fallback, guarded against homonyms across parts of speech (`arti` = "near"/adverb vs. "to plow"/verb; `stotis` = "station"/noun vs. "to become"/verb both got mistagged before the guard) by cross-checking Wiktionary's own part-of-speech listing — unless the app's **pre-existing** `Word.hint` POS tagging (`veiksmažodis`/`глагол` for verbs, discovered mid-build, not part of the original plan) already confirms the word's real sense, in which case that hint wins. Enrichment happens lazily at serve time — `verb_lookup.lazy_enrich_word()`/`lazy_enrich_words()` run right before a study/review queue is returned (`words.py`'s list/review-queue builders, `word_lists.py`'s personal-list detail), enriching only words a real request is about to show rather than the whole vocabulary table up front; cheap paths (curated match, hint-confirmed non-verb) always run, the slow Wiktionary path is capped by a small per-request budget so one request can't fan out into dozens of HTTP calls, with anything skipped simply retried next time that word is served. `backend/scripts/backfill_verb_forms.py` still exists as an optional pre-warming tool but is no longer load-bearing; `add_my_word` and the Chrome extension's word-save path enrich new verbs best-effort too (never blocks on a slow/unreachable Wiktionary). Shown only during study/quiz (`QuizSession.tsx`), never on the list/vocabulary pages, and only when it can't leak the answer: immediately on the flashcard/forward-MCQ/syllable-gap stages, and only after answering on the three stages where the Lithuanian word itself is the thing being elicited (reverse-MCQ, assemble, type). Per user feedback during testing, it renders as TAK's speech-bubble text (replacing `Prisimeni?`/`Pagalvok!`) rather than as card text — `PageMascot` gained a `forcePhrase` prop so the bubble's own mood-reaction system (`Puiku!`, `Hmm…`, …) doesn't override it the instant a session's mood leaves neutral, which happens on almost every answer. New tests: `backend/tests/test_verb_lookup.py`, `frontend/tests/verb-principal-forms.spec.ts`. See `documentation/verb-principal-forms.md` for the homonym/hint-tagging gotchas and `plans/improvements/implemented/IMPLEMENTED-plan_20_verb-principal-forms-study.md`. |
| 21 | 2026-09-11 | Fix #172: deleting a word from a personal list was broken for **every** user — the server returned 500 on every attempt with `ForeignKeyViolation: update or delete on table "word" violates constraint "word_list_item_word_id_fkey"`. `delete_my_word` (`backend/routers/word_lists.py`) calls `session.delete(item)` before `session.delete(word)`, but **the order you write is not the order SQLAlchemy emits**: it sorts deletes by dependency edges taken from ORM `relationship()` declarations, and `models.py` declares none, so it emitted `DELETE FROM word` first. Fixed with a `session.flush()` between the dependent deletes and the word delete. **Two things hid this.** (a) #171's fix (`7f2fc21`) had just made the failure *visible* ("Request failed") instead of silent, so #172 was triaged as a duplicate of #171 and closed as "no code fix needed" — the backend was never actually executed, only read. (b) The test suite runs on SQLite, which **does not enforce foreign keys unless asked**, so `test_edit_and_delete_word` passed the entire time the feature was 100% broken in production. Added `enforce_foreign_keys()` to `backend/conftest.py` (opt-in per test — enabling it globally fails ~27 existing tests whose fixtures carry dangling references; that cleanup is a separate follow-up) and `test_delete_word_does_not_violate_foreign_keys`, confirmed to fail without the fix. Also `frontend/tests/issue-172-delete-pasiulymas-list-307.spec.ts` as a UI guard, though it mocks the API and so cannot catch this class of bug. Since `models.py` has no relationships anywhere, **any** multi-table delete in this app is a candidate for the same bug — see `documentation/testing-foreign-keys.md`. Found by the user testing locally, after the automated pass reported it fixed. See `plans/triage/implemented/IMPLEMENTED-issue-172-delete-pasiulymas-list-307.md`. |
| 22 | 2026-09-11 | Fix #173: opening a personal word list took ~20s. **The triaged root cause was wrong and is worth knowing about** — it blamed the Wiktionary HTTP calls in `verb_lookup.py`, the only thing in that file that visibly does I/O, but measurement showed Wiktionary answers in 0.7–1.3s and cost almost nothing. The real cost was two N+1 round-trip patterns against the remote Neon DB, neither of which looks like I/O in the source: (a) `match_curated_verb()` issued an exact `SELECT` **plus** a cache-validating `SELECT COUNT(*)` *per word* against a 358-row static table that was already fully cached in memory — 56 words × 2 queries ≈ the reported 20s; fixed by building both the exact and accent-stripped indexes from one table scan and validating the row count once per `Session` (via `session.info`) instead of once per lookup. (b) `lazy_enrich_words()`'s commit expired every caller-held `Word`, so the caller's immediately-following serialization loop silently re-`SELECT`ed each row one at a time — 42.98s for list 178's 241 words, worse than the reported bug, and newly triggered on nearly every visit once (c) landed; fixed by disabling `expire_on_commit` around that one commit. (c) Separately, a word a live Wiktionary lookup actually ran for and missed now persists a `part_of_speech = "unknown"` sentinel instead of staying `NULL` forever, so the existing skip-gate stops retrying it every visit; the per-request Wiktionary budget dropped 3→1 and gained a 4s wall-clock deadline. Words never actually attempted are deliberately left unmarked. Measured `_list_words`: list 178 **45.2s → 1.7s**, list 316 **21.3s → 1.7s**; `GET /api/lists/178` over HTTP went from exceeding 60s to 1.8–4.0s. This also fixed 4 pre-existing Playwright failures (`issue-167`, `issue-170`, `navigation`) that were just this timeout in disguise. Guarded by statement-counting tests (`before_cursor_execute` listener, not wall-clock) in `backend/tests/test_verb_lookup.py`, both confirmed to fail against the pre-fix code, plus `frontend/tests/issue-173-study-list-load.spec.ts`. See `documentation/verb-principal-forms.md` and `plans/triage/implemented/IMPLEMENTED-issue-173-slow-study-list-wiktionary-lookup.md`. |
| 23 | 2026-09-12 | In-app inbox: an envelope with a Teams-style unread badge (filled circle/pill on the icon's top-right corner, `bg-destructive`, "99+" cap) sits left of the RU/EN switch in `Header.tsx`, opening a dropdown preview (latest 5, "Mark all as read", "All messages" → `/dashboard/inbox`, a minimal Gmail-style list: unread dot, sender, kind pill, snippet, Gmail-style date, message view with CTA/Reply/Delete+Undo). New tables only — `InboxMessage`, `InboxDelivery`, `UserAchievement` — no `User` column, so no Alembic migration (tables come from `create_all()`, see `documentation/inbox.md`). Superadmins compose bilingual broadcasts (Admin → Messages → Inbox) to selected users, all, premium, free, or inactive N+ days, with a dry-run recipient count, sent history and retract. Users automatically get once-only celebration messages for streak/words/CEFR/first-grammar-lesson/first-exam/100-phrases milestones, evaluated inside the already-running `GET /me/stats` with a grandfathering pass for pre-feature users (no retroactive flood) and an `ON CONFLICT … RETURNING` race guard so two concurrent stats calls can't double-send. Also mirrors three existing outbound flows into the inbox: the weekly leaderboard reward/notice, report-status changes (sent regardless of email consent), and Premium activation — the Stripe `checkout.session.completed` welcome is written in its own try/commit/rollback **after** the existing entitlement commit so a failed inbox write can never roll back a paid Premium grant or make the webhook return non-200. Every read this feature adds (unread count, list, detail, achievement ledger, CEFR thresholds) goes through #24's `backend/cache.py` rather than hitting Neon, narrowed with per-user `cache_tags` so a broadcast only evicts its recipients' entries — a warm `GET /me/inbox*` issues zero SQL statements, guarded by statement-count tests in `backend/tests/test_inbox.py`. New Playwright coverage: `inbox.spec.ts` (badge visual contract, request-avoidance), `inbox-buttons.spec.ts` (the button contract — one test per control, plus "nothing extra is rendered" at both 1280px and 375px), `admin-inbox-composer.spec.ts`. See `documentation/inbox.md` and `plans/improvements/active/plan_23_inbox.md`. |
| 24 | 2026-09-11 | App-wide read caching to cut Neon network transfer, the resource #15 exhausted. The database is tiny — the cost was re-reading the same small tables on every request: the whole 1.3MB `verb` table (with four `json.loads` per row) on *every* verb-lesson task request, full `Article` rows **including bodies** to render the footer's few titles on every page load, every `PracticeTest` row (~65KB, `lesson_text_lt` included) just to count tests, and one `User` SELECT on every authenticated request. New stdlib-only `backend/cache.py` — a `threading.Lock`-guarded dict, no Redis, no new dependency — is the single mechanism: `get_or_load(key, loader, tags=…, ttl=600, store_if=…)`, values are plain data (dict/tuple/`SimpleNamespace`, never live ORM instances) and **every read returns a `copy.deepcopy`**, which is what lets `/lists/{id}` bolt a per-user `status` onto cached word dicts without leaking it into the next user's response. **Invalidation is inferred from the SQL the app actually commits**, not written by hand at each write site, so a future admin edit path can't forget to clear a cache: class-level `Session` listeners turn an ORM flush into `t` + `t:pk=<pk>` + `t:user=<user_id>`, and Core/textual DML (which can't say which rows it touched) into `t:*`, narrowable via `.execution_options(cache_tags={…})`. The asymmetry is deliberate — a single-row flush emits a bare `t` that clears only whole-table entries, so one user's answer doesn't wipe every other user's per-user entry, while a bulk `UPDATE` clears the whole `t` family. Tags are applied on **`after_commit`** and discarded on `after_rollback`, never before the commit: evicting early would let a concurrent reader load the pre-commit rows and re-cache them *after* the eviction. The remaining race (reader loads old rows, writer commits and evicts, reader stores) is closed by a per-table version counter snapshotted before the loader runs — if it moved, the value is served but not stored. ~24 loaders across `grammar_service.py` and the words/articles/news/phrases/practice/constitution/grammar/admin routers; per-user joins, `random.sample`, `now` comparisons and every permission/visibility check (`_can_access_list`, test `status`/`is_admin`, premium) still run per request, on cached data rather than instead of it. The highest-risk piece is the auth user row (`auth.py`): the column values are cached for 60s, then per request rebuilt into a `User`, `make_transient_to_detached()`, `session.merge(load=False)` — a session-attached object with **no SELECT** that still marks dirty and flushes normally, and whose flush evicts `user:pk=<id>`; a granted/revoked admin or premium flag is therefore visible on the very next request, with the 60s TTL bounding only the *other* instance during a zero-downtime deploy. **Deliberately not cached** (see `documentation/caching.md`): progress rows, `/me/stats`, streak, activity calendar, due counts, review and continue-session pools — anything that changes on every answer or depends on `now` — plus admin GETs in general (the public CEFR-thresholds GET is the one exception). TTL (10 min default) is the safety net for writes this process can't see: seed/backfill scripts and Alembic run from another process, so **after a seed script against production, changes appear within 10 minutes or on restart**. Known ceilings, all documented: one process only (`render.yaml` runs a single uvicorn worker); no row-level tags for `word`, so any word write evicts every word-list entry; `_list_words` refuses to cache a list containing a word whose `part_of_speech` is still NULL, so `lazy_enrich_words` (#22) keeps getting a chance to run. New tests: `backend/tests/test_cache.py` (tag semantics, deepcopy isolation, rollback, the stale-refill race, TTL, size cap) and `backend/tests/test_cache_endpoints.py` (warm-call statement counts — `/api/news`, `/api/footer-articles`, `/api/articles`, `/api/me/quota`, `/api/practice/categories`, the exam pools and `/api/grammar/lessons` all hit the DB **zero** times when warm, auth lookup included — plus invalidation driven through the real admin/user write endpoints, and cross-user isolation). See `documentation/caching.md` and `plans/improvements/active/plan_24_neon-transfer-caching.md`. Post-deploy Neon "Data transfer" before/after numbers still to be recorded in `documentation/caching.md`. |
| 25 | 2026-09-13 | Premium conversion, pass 1 — the daily-limit wall became an actual offer and the free limit dropped 10 → 5. Measured first (production, read-only): 148 users, 111 ever studied, 31 ever hit the old wall, but only **5 ever reached Stripe Checkout and 3 pay**. Checkout converts fine (3 of 5); the losses are upstream. Two fixes: (a) `DAILY_LIMIT` 10 → 5 — free-user daily sessions are p50=3 / p75=9 / p90=10, so at 10 the wall almost never fired; at 5 the median free day (3 sessions) is still untouched while users blocked at least once in 90 days rise 16 → 27. 3 was rejected because it blocks the median day and day-1 retention is already the weakest step (57 of 111 studied on exactly one day). (b) `DailyLimitBanner` at 0 remaining is no longer a thin line of text with a small link next to greyed-out buttons — 28 users hit that and never once saw Checkout — but a real offer card: title, three unlocks, one full-strength button carrying the €4.50 price. At exactly 1 session remaining the old thin nudge is unchanged (that user can still study). Backend tests now import `DAILY_LIMIT` instead of hardcoding 10, so the number can be re-tuned in one line. Also found and **not** yet fixed: the weekly leaderboard reward hands 7 days of Premium to the same daily-active top 3 every week — 12 of the 15 most active users hold Premium and 9 have no Stripe customer at all. See `documentation/premium-conversion.md` and `plans/improvements/active/plan_25_paywall-offer.md`. |
| 26 | 2026-09-13 | Grammar «Напомни что я мог забыть» — a mixed 10-task review run built from every «Повторение» (practice-level) lesson the user has already passed, in grammar programs (cases + verbs) they're enrolled in. New `GET /api/grammar/remind/tasks`: eligibility (passed + practice + enrolled) is checked before the daily-quota charge, so a 404 (`no_passed_practice`) never spends a session; otherwise up to 10 eligible lessons are sampled and their tasks flattened/shuffled into one run. The result is saved through the *existing* `POST /grammar/lessons/{id}/results` against a sentinel `REMIND_LESSON_ID = 0` (real lesson ids start at 1) — a new table was rejected as needing a model, two more streak unions and a user-delete cascade for no real benefit. Three readers that count "lessons passed" now skip the sentinel explicitly: `words.py` `get_stats`'s `lesson_best` subquery, `admin.py`'s per-user stats loop, and `grammar.py`'s `GET /grammar/progress` — streak and the activity calendar are left untouched, since a remind run *should* count as a study day. `ProgressStatCard`'s `primaryAction` now also accepts an in-place `onClick` (button, with `disabled`/`hint`) alongside its original `href` (`Link`) form, reused by the Грамматика hero card: the button is hidden entirely at zero lessons passed (existing `count > 0` rule) and shows disabled + a hint once ≥1 lesson is passed but none at practice level. See `documentation/grammar-remind.md` and `plans/improvements/active/plan_26_grammar-remind.md`. |
| 27 | 2026-09-14 | Fix #175: clicking the header envelope could show a blank dropdown with no explanation. Root cause was in `InboxMenu.tsx`, not the backend (confirmed by running the endpoints against a zero-delivery user) — three body states (cold-Neon loading, a swallowed fetch error, and a genuinely empty inbox) all rendered as a thin, easy-to-miss line or nothing at all. Loading now shows one skeleton row (`inbox-dropdown-loading`, matching `/dashboard/inbox`'s pattern); a failed fetch is tracked (`failed` state, no longer silently caught) and, combined with empty, renders as one centred 13.5px message (`inbox-dropdown-empty`) using the existing `inbox.empty` / `inbox.loadError` i18n keys — reopening the dropdown after a failure retries since `items` stays `null`. Request discipline (no fetch on navigation, 60s reuse) is unchanged; the unread-count badge refresh is still silent by design. See `plans/triage/implemented/IMPLEMENTED-issue-175-inbox-dropdown-blank-state.md`. |
| 28 | 2026-09-14 | Admin panel: the Users table (`/dashboard/admin`) headers — User, Plan, Premium until, Sessions today, Last login — are now clickable to sort, toggling asc/desc on repeat clicks (nulls always sort last). Client-side only, sorts the already-fetched `filteredUsers` array before pagination; no API/backend change. Scoped to the Users grid, the one admins scan/sort most — the other admin tables (reports, articles, feedback, practice questions) weren't touched. New test: `frontend/tests/admin-users-sort.spec.ts`. |
| 29 | 2026-09-14 | The landing-page leaderboard widget (`Leaderboard.tsx`) only ever showed the top 10 — a user outside it saw nothing about their own score at all. `GET /api/leaderboard` now returns `{entries, me: {rank, score}}` instead of a bare array: `me` is computed with two small extra queries reusing the existing `build_leaderboard_score_joins()`/`LEADERBOARD_SCORE_EXPR` from `leaderboard_service.py` (score for the calling user, then a count of users scoring higher for rank — `null` rank when score is 0, no fake position). The cached top-10 `_load()` is untouched. The widget renders one extra "me" row below the top 10 only when the user isn't already visible there (avoids a duplicate). Scope was deliberately kept to this one widget — no new dashboard page/route. New: `backend/tests/test_leaderboard_me.py`; extended `frontend/tests/leaderboard.spec.ts` (+3 cases) and updated 2 unrelated specs' mocks for the new response shape. Also fixed an unrelated pre-existing failure surfaced by the full suite run: `quota.spec.ts` still asserted the pricing page's old "10 sessions/day" copy, stale since #25 dropped the free daily limit to 5. See `plans/improvements/implemented/IMPLEMENTED-plan_29_leaderboard-my-score.md`. |
| 30 | 2026-09-16 | Number keys pick an option. Anywhere a study session shows a short list of buttons, `1`–`9` now select them top-to-bottom: the stage-1 flashcard's self-evaluation (`1` = «С трудом», `2` = «Легко»), the two multiple-choice stages in `QuizSession` (2 and '2r'), and the fill-word MCQ in `PhraseSession`. One shared hook, `frontend/lib/useNumberKeys.ts`, so the shortcut means the same thing on every surface; it ignores digits typed into an input/textarea (answer fields, the feedback modal) and any key with a modifier. Tile assembly is deliberately excluded — order matters there and the pools run past 9 tiles. Every option prints its number as an `aria-hidden`, desktop-only badge before the label (see "Option number badge" in the component library) — `aria-hidden` keeps the accessible name equal to the label, so `getByRole` matchers and screen readers are untouched, but `textContent` now reads `"1Легко"`, so specs comparing raw button text use the new `stripBadge()` helper. Covered by `frontend/tests/keyboard-option-select.spec.ts`; see `documentation/keyboard-shortcuts.md`. |
| 31 | 2026-09-16 | The weekly leaderboard reward stopped being a free subscription for the users most likely to pay. Measured first (production, read-only): 144 users, 15 with `is_premium`, but only 3 ever reached Stripe and **2 pay**; `prepared_message` showed **48 reward grants to 12 users, 4 of whom took 36 of them** (`norpus1` won 11 of ~17 weeks), and **5 of the 10 most active users held Premium having never paid**. Two causes, both fixed: (a) a flat 7 days for all three places — a weekly grant of N days covers N/7 of the calendar, so 7 days/week is *continuous* Premium for a near-stable top 3. Now `leaderboard_service.REWARD_DAYS = {1: 7, 2: 4, 3: 2}` (user's call; 5 for 1st was proposed and rejected), so 2nd and 3rd meet the paywall 3 and 5 days a week. (b) the grant was additive (`premium_until += 7 days`), letting repeat winners bank runway — new `grant_reward_premium()` rolls `premium_until` to `now + days` and never further, and is used by **both** send paths (`scheduler.send_weekly_rewards`, `admin.send_prepared_message`) so they can't drift. It returns 0 and touches nothing for an unlimited admin grant (`premium_until IS NULL`), which the old code would have silently downgraded to a week. Deliberately the opposite of `billing._extend_premium`'s `max()`: paid time is bought and must be honoured, reward time is granted and must expire — the two stay separate. Also fixed a real double-grant: on 2026-09-14 all three winners got **two** reward rows 0.6s apart at exactly 10:00 (two Render instances overlapping during a deploy, each running its own APScheduler; `already_generated` is a read-then-write race) and two winners were granted 14 days instead of 7. New `PreparedMessage.reward_rank`/`.rewarded_week` + a partial UNIQUE index on `(user_id, message_type, rewarded_week)` declared in **both** migration `b8c9d0e1f2a3` and `__table_args__` (`database.py` builds schema from model metadata via `create_all()`), with the generator's insert in a savepoint that skips on `IntegrityError`. No backfill and no clawback — the two over-granted users keep their extra days by decision. Email copy now reads its day count from `REWARD_DAYS` (correct Russian plurals via `_ru_days()`), so the promise can't disagree with the entitlement. New `backend/tests/test_reward_grant.py` (15 cases); 562 backend tests pass. Evidence article published the same day (31d): `why-review-beats-new-words`, article id 34, `learning_materials`, bilingual RU+EN, carrying six DOI-verified citations (Cepeda 2006 & 2008, Karpicke & Roediger 2008, Roediger & Karpicke 2006, Lindsey 2014, Pavlik & Anderson 2008). It exists so the future review-first nudge can link to evidence instead of asserting science in UI copy — `documentation/spaced-repetition-evidence.md` records both what those papers support and the claims we must *not* make (there is no published 20%-overdue threshold). Review-first landed too (31c), as a **screen with a choice** rather than a silent clamp: when a list holds more due words than fit in one session, the study page shows a full-screen offer with two buttons — «Повторить 47 слов» (primary, `?mode=review` → a review-only session) and «Всё равно учить новое» (secondary → the user's ordinary mix, untouched). An earlier draft quietly capped the new-word share at 0.2 whenever the backlog was large; it was dropped once there was a button labelled "learn new", because a clamp behind it would make the label a lie. The offer reads `GET /lists/{id}/progress`, which now returns `due` and `session_size` — **not** `/study`, which charges a daily session: showing someone a recommendation must not cost one of their five, and a test asserts `sessions_today` is unchanged after two `/progress` calls. The trigger is a **count** (`due > session_size`), never a percentage: 20% of 20 scheduled words is 4, 20% of 1000 is 200. After «учить новое» the session runs normally but keeps a one-line reminder strip above it. Both study strips are now wrapped in `bg-slate-50` — `QuizSession` paints that over the body's green gradient from its own top, so a strip above it showed the gradient and read as a coloured band seamed against the session (this also fixed the pre-existing `more-new-at-higher-level` strip). `PageMascot` gets no `mood` here: at any non-neutral mood it swaps the page's phrase for a canned cheer. Copy is «Сегодня лучше заняться повторением» / "Today is better spent reviewing"; all Russian declension runs through the repo's existing `plural()` helper, since one hardcoded string gives "Повторить 21 слов". No scientific claim appears in any UI copy — the article is one link from both surfaces. Measured reach before shipping: the screen fires for 18 of 22 weekly-active users and on 36% of their user/list pairs, **every time** — a once-per-day suppression gate was proposed and deliberately rejected (2026-09-16): quality of learning over volume of sessions, and the person opening a third backlogged list is precisely who the recommendation is for. Repetition is the feature; do not "fix" it. New `backend/tests/test_review_first.py` (11) and `frontend/tests/review-first.spec.ts` (11, incl. what each button actually requests, declension, English, and 375px in both languages); 573 backend tests and 12 design-system-parity cases pass. Component library gains "Pre-session choice screen" and a background note on "Study notice strip". The rank column is `reward_rank`, **not** `rank`: Postgres reads `prepared_message.rank` as the functional-notation call `rank(prepared_message)` and fails every select of the table with "WITHIN GROUP is required for ordered-set aggregate rank". SQLite has no such function, so all 571 tests passed while the admin messages page 500'd against Neon — found by hitting the endpoint against the real DB, not by the suite. Written up in `documentation/testing-foreign-keys.md`. **Still open: `alembic upgrade head` against production — the reward columns from 31a are not in the prod schema yet, so nothing here is live until it deploys.** The originally proposed "block new words at >20% overdue, Premium bypass" was measured and rejected — it fires for 60 of 67 active users and reaches only 6 free weekly-active users. See `documentation/premium-conversion.md`, `documentation/spaced-repetition-evidence.md` and `plans/improvements/implemented/IMPLEMENTED-plan_31_review-first-and-premium-leaks.md`. |
| 32 | 2026-09-16 | One Premium offer everywhere, sold in place. Audited every upsell first (screenshots in `temp_files/screenshots/premium-upsell-audit/`) and found **four different designs for one product, only two of which named the price**: the flat green card on `/dashboard/lists`, an orange `amber-500` pill on an amber gradient card right next to it, the dark-button 429 screen, and `/dashboard/premium` selling through **`mailto:`** — "обратитесь к администратору… активируем вручную в течение 24 часов" — although Stripe Checkout has been live since #11 (production `GET /api/billing/config` → `{"enabled": true}`). That page had exactly one entry point, `practice/[id]` pushing to it when a free user opened a Premium test, and it failed three ways: it said «этот тест» about a test no longer on screen (no name or id was passed), its «Назад к практике» went to the category list rather than the test, and it sold by email. New shared `PremiumOfferCard` (title/subtitle/perks as props; shape, green button and price line fixed; `data-testid` root with derived `-cta`/`-price`) now backs both `DailyLimitBanner` at 0 remaining and a new **inline** paywall on `practice/[id]` — the test list stays on screen and the URL never changes. Practice gets its own perks led by "Все практические тесты без ограничений", since the generic word-list perks read as a non-sequitur to someone who just opened a test. `frontend/app/dashboard/premium/` is deleted. Price moved out of the button label into a line beneath it: it must stay on the card before the click (#25 measured 28 users hitting a priceless wall and none reaching Checkout) but a button should name the action, not the terms, and the old label wrapped at 375px. Copy rewritten across both surfaces — two short sentences instead of one long one with «И…» appended, and the support line standing alone: «Подписки оплачивают серверы и развитие Fluent.» New `frontend/tests/practice-premium-wall.spec.ts` (6 cases incl. URL unchanged, price placement, English, 375px in both languages, and `/dashboard/premium` returning 404); `daily-limit-banner.spec.ts` updated for the derived ids. 35 frontend tests pass across the four affected specs. Component library gains "PremiumOfferCard — the one Premium offer". **Not fixed, recorded in the audit:** the orange "Создавайте свои списки" card and the cookie banner are both still off-system. See `plans/improvements/implemented/IMPLEMENTED-plan_32_one-premium-offer.md`. |
| 34 | 2026-09-16 | Browser extension visual redesign — brought `extension/` (popup, options, on-page save icon/card) onto the shipped token palette (`ink`/`muted`/`line`/`emerald600`/`emerald700`) and the real TAK mascot, dropping every ad-hoc hex color the folder had accumulated (`#006A44`, `#10b981`, `#FFB81C`, `#C1272D`, plus assorted `#111827`/`#6b7280`/`#374151`/`#e5e7eb`/`#d1d5db` neutrals). New `extension/theme.js` — one shared, build-step-free global (`FLUENT_THEME` constants + a `takBareSVG(size)` helper reproducing `Tak.tsx`'s `bare` mode) — included via `<script>` in `popup.html`/`options.html` and prepended to `manifest.json`'s `content_scripts.js` so `content.js` can use the same globals; manifest bumped to 1.3.0. The three ad-hoc CTA colors (connect, upgrade, add) all collapse to `emerald600` — no other primary CTA in the app is red, and gold was never a real second brand color, just an unreviewed pick. New `getStats` message in `background.js` (mirrors the existing `getLists` proxy) proxies the already-tested `GET /me/stats`; the popup now shows "N words saved" and a "Review N words" button, shown only when `due_review > 0` so it's never a dead CTA — no per-site count or CEFR progress line, since neither has any backing data anywhere. TAK's `bare` mark replaces the plain "f." dot in the popup header, options header, and the on-page collapsed icon; a `tak-bounce` keyframe (mirroring `frontend/app/globals.css`'s naming) plays once on the on-page card's "Added!" button-text swap, injected into the content script's closed shadow root (which had zero shared CSS before this — every color was a literal per-element hex). Two deliberate deviations recorded in the component library: the on-page floating icon/card keep `box-shadow` (unlike flat in-app cards) because they render over arbitrary, unpredictable third-party page backgrounds and need their own elevation to read as floating UI at all; and the extension keeps `system-ui` rather than Inter, since a content script injecting a web font onto third-party pages adds latency and can hit page CSP. No backend changes — `/me/stats` already existed and was already tested. No RU/EN or 375px check applies: the extension UI has no i18n (English-only by design) and isn't a responsive web surface (fixed-width Chrome popup + desktop content-script overlay). See `plans/improvements/active/plan_34_extension-redesign.md`. |
| 35 | 2026-09-16 | Extension redesign #34's token/color choice was rejected — the user wanted `extension/` to pixel-match a separate, pre-existing mockup (`temp_files/redesign extention/Fluent Extension.dc.html`) instead of the site's shipped `ink`/`muted`/`emerald-600` palette. `extension/theme.js`'s `FLUENT_THEME` now holds that mockup's own "modernist" tokens (`bg #f3f2f2`, `surface #eae9e9`, `text #201e1d`, `accent #ec3013`/`#dd2b0f`/`#ae1800`, `divider`/`muted` as alpha-blended text, `0px` radius everywhere, Archivo 400/800), which happen to be the exact same hex as `Tak.tsx`'s fixed `BODY`/`LIMB`/`EYE_BG` brand constants — a deliberate coincidence, not a new color introduced for TAK. Two new pose helpers, `takFloatSVG`/`takWaveSVG`, mirror the mockup's idle-float and arms-up-celebration (native SVG `<animateTransform>`) poses; `takBareSVG` is unchanged. `popup.html`/`options.html` load Archivo from Google Fonts directly; `content.js` bundles two local `.woff2` files (`extension/fonts/`, latin + latin-ext subsets) instead — a remote font `<link>` inside an arbitrary third-party page is subject to that page's CSP — referenced via `chrome.runtime.getURL()` inside the closed shadow root's own `<style>`, declared in `manifest.json`'s new `web_accessible_resources` (bumped to 1.4.0). New `getCefrThresholds` proxy in `background.js` (mirrors `getStats`) backs the popup's real level pill + progress bar: current level is the highest `GET /api/admin/settings/cefr-thresholds` entry whose threshold is at or below the user's total words-saved figure, next level is the following entry, and the progress bar/next-level line disappear entirely once already at the top level. That total — also the popup's headline "words saved" stat and the saved-confirmation ordinal — deliberately sums `word_count` across `GET /me/word-lists` (already fetched via the existing `getLists` proxy) rather than using `/me/stats`'s `known+learning`: caught during validation that adding a word via the extension creates only a `WordListItem`, never a `UserWordProgress` row, so `known+learning` never actually moves when you save one — it would've shown a stale figure forever until the word was studied elsewhere. The popup's stat block also replaces the old free/premium badge (premium gating never applied to stats/review, only the on-page add action) and keeps "total words saved" (not "saved from this site" — no per-site tracking exists and the user didn't want it built). The on-page collapsed dot becomes a labeled pill (TAK nod icon + "Save" + a real `⌥S` hint backed by a new `Alt+S`-on-selection keydown listener, matched on `e.code` so macOS's Option+S `ß` doesn't break it); the expanded "Add card" gets a 2px `text`-bordered header with a floating TAK mark and the stressed syllable underlined in `accent` instead of `emerald-600`. The saved-confirmation state is a full card replacement, not a button-text swap: TAK's wave pose + "Saved!" + a real ordinal ("Nth word saved", re-read from `/me/word-lists` after the add) for a genuinely new word, or "Already saved" + its location for `already_added` — one shape for both, a single "Open list" button, no Undo (the mockup has one; the user's explicit call, even though `DELETE /me/word-lists/words/{id}` exists and would make it honest). The mockup's "Highlight saved words on this page" button is relabeled "Open my vocabulary" and points at `/dashboard/vocabulary` — `content.js` has no page-scanning/highlighting feature and building one was ruled out of scope for a redesign; keeping the old label pointing at a new action would have been dishonest UI. Component library's two #34 "deliberate deviation" rows (floating-shadow, system-ui-not-Inter) are replaced by one corrected entry recording the extension's now fully separate palette. See `plans/improvements/active/plan_35_extension-mockup-match.md`. |
| 36 | 2026-09-16 | Three small extension follow-ups on top of #35. (1) **Dev-only backend picker**: the whole "which backend" section of Options (Production/Local dev) and the small env label at the top of the popup are now hidden for everyone but the developer — there's no Chrome Web Store build to gate on (everyone loads the same unpacked zip), so `background.js`'s `connect()` checks the connected email against a hardcoded `DEV_EMAIL` and sets `devUnlocked` in `chrome.storage.local` on a match, a one-way flag never cleared automatically. `getStatus()` now returns it; `options.js` hides `#backend-section` and `popup.js` skips the env pill unless it's true. A real user only ever has one backend, so surfacing "Production" as if it were a live choice was just confusing chrome for them. (2) **New extension icon**: the old icon (a Facebook-style "f" over a Lithuanian tricolor flag, unrelated to any current branding) is replaced with TAK's real bare mark on the same rounded `#f3f2f2` tile as `frontend/public/favicon.svg` — rendered at 16/32/48/128px straight from that SVG via a throwaway Playwright script (no `rsvg-convert`/ImageMagick available in this environment), so the extension's toolbar icon now matches the site's own favicon instead of using unrelated flag/logo art. (3) **"Add" not "Save"**: the on-page collapsed pill's label is renamed from "Save" to "Add", matching the expanded card's own "Add to learn"/"Add "word"" wording one click later in the same flow — same `⌥S` shortcut, just consistent terminology. Manifest bumped to 1.5.0. No backend changes; `pytest` (52 + 573) and `node --check` all pass. Screenshots: `temp_files/screenshots/plan_36_extension-dev-gating-icon/` (locked vs. unlocked popup/options) and the refreshed `ext-collapsed-pill.png` under `plan_35_extension-mockup-match/`. |
| 38 | 2026-09-22 | **Word audio — local prototype only (not deployed, not merged; branch `proto/word-audio`).** Premium/admin words get pronunciation via a new `GET /api/audio?text=`, which generates lazily per lesson and caches in three layers: disk `backend/.audio_cache/` standing in for the production DB, because local dev hits the production Neon DB so no table could be added; a 50 MB in-memory byte-capped LRU; and the browser via `private, no-cache` + an `ETag` equal to the cache key, which returns an empty 304 when unchanged. Voice pivoted from ElevenLabs v3 (read `jausti` as «джаусти») to Azure `lt-LT-LeonasNeural`, via `edge-tts` locally. **Production must use the official Azure S0 key.** Stress marks (U+0300/0301/0303) are stripped before TTS. the config `backend/data/pronunciation.json` respells misread words for the voice only. Its first and only entry, `siųsti→sjūsti`, came from the user listening through the lists. UI: `SpeakButton` beside the word in lesson stages 1–2, prefetched per lesson; autoplay default ON, with a settings checkbox and an in-card RU/EN-style switch (`localStorage`, not the DB); click-only buttons on `/dashboard/lists/[id]` and `/dashboard/vocabulary`. Free-user upsell decided but not built: a small amber «Послушать в Premium» pill above the card opening `PremiumOfferCard` in place. Tests: `backend/tests/test_audio.py` (16), `frontend/tests/audio-button.spec.ts` (34). See `documentation/audio.md` and `plans/improvements/implemented/IMPLEMENTED-plan_38_word-audio-prototype.md`. |
| 41 | 2026-09-22 | Production login outage fix. The #39 deploy (`c405090`) let Render install `sqlmodel` 0.0.46 (requirements were unpinned), which rejects naive datetimes on write and returns aware ones on read: the OAuth callback failed on `last_login` (nobody could log in) and `GET /api/me/stats` crashed in `is_premium_active`. Rolled back on Render, then pinned every direct dependency in `backend/requirements.txt` to the versions the 2026-09-17 build ran (`sqlmodel==0.0.42`) and synced the local `.venv` to them; 617 backend tests pass on those versions. See `documentation/deploy-render.md` ("Pinned Python dependencies"). |
| 40 | 2026-09-22 | Dead-code cleanup. Deleted the pre-refactor `api/` app (24 tracked files; nothing imported it, since the root `main.py` and `start.sh` boot `backend/main.py`; its `.env`/`.venv` were only symlinks into `backend/`), and the dead `scripts/seed.py`, whose call was also removed from `render.yaml`'s `buildCommand`. It dated from the old `api/` layout. Before reseeding it **deleted every `UserWordProgress`, `Word`, `WordList` and `WordListItem` row**, and through the `api/.env → backend/.env` symlink it pointed at the production DB. It survived only because one import (`api/data/vocabulary.py`) no longer existed. Render never ran it: the live Build Command (deploy log, 2026-09-17) is `npm --prefix frontend install && npm --prefix frontend run build && pip install -r requirements.txt`, with no seed step, so `render.yaml` is not what production uses. Found by the independent audit of plan #39. See `documentation/grammar-sentence-data-integrity.md` ("Data ownership"). |
| 39 | 2026-09-22 | **Word audio in production (deployed 2026-09-22 as `d49a0a1`, builds on #38).** `GET /api/audio?text=` now calls the official Azure Speech REST API (`lt-LT-LeonasNeural`, paid S0 key, `AZURE_SPEECH_KEY`/`AZURE_SPEECH_REGION`) with XML-escaped SSML and a 10s timeout, and stores clips in a new `audio_clip` table (created by `create_all()`, no Alembic) behind the 50 MB in-memory LRU and the ETag/304. Only the text of an existing, non-archived `Word` (≤ 60 chars) is ever synthesized (404 otherwise); monthly caps `AUDIO_MONTHLY_CHAR_CAP` (200k ≈ $3) and `AUDIO_MONTHLY_BYTE_CAP` (50 MB) are the hard stop, with a once-a-month Telegram alert. The miss path ends its transaction before waiting, a non-blocking `BoundedSemaphore(4)` and a 10s lock timeout return 503 instead of pinning threads, and the insert is `ON CONFLICT DO NOTHING`. Removed: `edge-tts`, the ElevenLabs path, the disk cache. Free users now see a locked speaker (lesson card stages 1–2, list pages) and, on the lesson card, a «Послушать в Premium» / "Listen with Premium" amber pill, both → `/pricing` (new tab inside a lesson); a failed quota fetch shows no audio control at all. `/pricing` and the daily-limit wall list «Произношение каждого слова» / "Pronunciation of every word"; the Settings autoplay «Premium» tag is now the amber `/pricing` link. Also a one-off consent-only announcement script (`backend/scripts/send_audio_announcement.py`, dry run by default, append-only ledger file, never `PreparedMessage`), and conftest guards so tests can never bill Azure or send mail. Plan `plan_39_word-audio-production.md`; details, audit and purge query in `documentation/audio.md`. Follow-ups the same day: the autoplay switch now shows on every lesson stage (right edge of the mascot row; stage 1 keeps it on the card), so sound can be turned off mid-lesson; the lesson header's back link and counters no longer wrap at 375px under the long review label. The listening article became `lithuanian-pronunciation`, aimed at the Google query «литовское произношение» (`documentation/articles-seo.md`). |
| 42 | 2026-09-23 | Admin panel now shows paying subscribers, not just the head count. A new **«Платные» / "Paid"** chip in the Users filter row carries an emerald badge with the number of users whose `subscription_status === 'active'`, i.e. who have a live Stripe subscription. The count first sat next to the Users sub-tab's grey head count and was moved onto the chip — two bare numbers on one tab needed tooltips to be legible at all, while on the chip the label already says what the number is (and the `admin.countAllUsers`/`admin.countPaidUsers` tooltip strings went away with it). Deliberately **not** `premium_active`: production today has 154 users, 22 `is_premium`, 14 premium-active but only 10 with an active subscription — admin-granted and expiring premium would inflate the number that's supposed to read as revenue. No backend change: `GET /api/admin/users` already returns `subscription_status` per row, so this is a client-side count in `frontend/app/dashboard/admin/page.tsx`. Both badges got `title` tooltips (`admin.countAllUsers`, `admin.countPaidUsers`, RU + EN) since two bare numbers side by side aren't self-explanatory. The chip filters the table to those users and highlights in emerald rather than the row's grey (`all | paid | active | inactive | deletion`); the row gained `flex-wrap` because a 5th chip overflows at 375px. The long label ternary became a `Record<UserActivityFilter, string>`. **No chart was built**: a paying-users-over-time graph needs subscription history and nothing stores it — `user` holds only the current `subscription_status`, there is no events table, and past months can't be backfilled from it. New `frontend/tests/admin-paid-subscribers-badge.spec.ts` (6 tests: the count excludes granted premium, the filter shows only the 2 subscribers, plus RU/EN × desktop/375px screenshots, unfiltered and filtered, in `temp_files/screenshots/plan_42_admin-paid-subscribers-badge/`). Those shots needed `animations: 'disabled'` — see `documentation/local-dev-gotchas.md` ("Screenshots catch mid-transition UI"). |
| 37 | 2026-09-23 | Low-effort SEO fixes driven by a 3-month GSC report (86 clicks / 2,211 impressions / 3.9% CTR): pages rank, people don't click. `frontend/app/dashboard/articles/[slug]/layout.tsx`'s meta description no longer starts by repeating the article's own `<title>` — every article's `body_ru` opens with a `# Heading` line that the old strip-then-slice logic left in as text, eating the 160-char budget before the real content started; now the leading heading line is dropped and whitespace is collapsed before slicing, fixing the bug for **every** article, not just the two flagged in GSC. `backend/main.py`'s sitemap gained `<lastmod>{today}</lastmod>` on the `/programs/{key}/` and `/dashboard/phrases/{id}/` loops, matching the `static_pages`/`articles` blocks that already had one (flagged in the Sep indexing check as a likely non-indexing signal for 5 of 8 program pages). New `frontend/app/dashboard/phrases/layout.tsx` (pattern-matched to `grammar/layout.tsx`) gives `/dashboard/phrases/` real title/description/canonical/`openGraph` — it had none before, falling through to the generic site-wide default despite `lithuanian phrases` being a live query with 0 clicks. Content: rewrote the opening paragraph of `is-lithuanian-hard-to-learn` and `numbers-01-basics` (`body_ru` + a parity `body_en` sentence, both DB `UPDATE`s via psycopg3) into a tighter, self-contained hook — sized, character-by-character against the actual `generateMetadata` transform, so the 160-char slice lands exactly on the paragraph's own final period instead of cutting mid-word; `numbers-01-basics`'s `title_ru` also gained "цифры" (plural, matching the top query "цифры на литовском, 35 impressions — the body previously only had the singular dative "цифре"). Added one inline link each: `regitra-vocabulary` → `/programs/regitra/` (an indexed-but-unlinked list page, 61 impressions vs. 4 for the article) and `common-lithuanian-words` → `/dashboard/phrases/`, in `body_ru` (and `body_en` for the words article — see below). New `documentation/seo.md` records the H1-eats-the-budget pattern (including the character-budget-crafting technique used for the rewrites) and the pre-existing RU-only-metadata limitation (one static URL per article, no `hreflang`, so an EN visitor's toggle never changes what Google indexes); `documentation/articles-seo.md` got a one-line accuracy update to match. `regitra-vocabulary`'s `body_en` column turned out to be completely empty in the DB — a pre-existing gap, not something this plan introduced, meaning EN visitors on that article previously saw a blank body regardless of the link. Rather than skip the EN link, translated the full 16KB `body_ru` into a matching English `body_en` (vocab tables drop the Russian column, matching the convention already used elsewhere), so the `/programs/regitra/` link now exists in both languages and the article has an English version at all for the first time. Also caught and fixed a follow-up gap during the content read-through: the `numbers-01-basics` SQL rewrite updated `title_ru` to add "цифры" but left the on-page `body_ru` H1 line reading the old title — a second `UPDATE` synced the H1 to match. See `plans/improvements/active/plan_37_seo-quick-wins.md`. |
| 43 | 2026-09-23 | Phrase audio: `SpeakButton`/`LockedSpeakButton` on `/dashboard/phrases/[id]` and every stage of `PhraseSession` (`/dashboard/phrases/[id]/study` and `/dashboard/phrases/review`) — same Premium/admin gating, `AutoplayToggle`, and `AudioPremiumPill` upsell already shipped for words (#39), reusing the components with no new pattern. `backend/routers/audio.py`'s `max_length` raised 60→200 (a full sentence often exceeds the old word-catalogue cap) and the miss-path existence guard now also accepts `Phrase.text`/`CustomPhrase.text` alongside `Word.lithuanian`, still 404 on anything the app doesn't own. New `backend/data/phrase_pronunciation.json`, merged into the same respell pass as `pronunciation.json` but kept as a separate file since phrase-level fixes are expected to diverge from word-level ones — first entry fixes "sumuštinis" (sandwich) and its case forms, which the voice misread; A/B testing found stress diacritics have **no effect at all** on this voice, the fix that worked was a real respelling (`sumuštin` → `sumu štin`, split after the stressed syllable). New skill `.claude/skills/tune-pronunciation/SKILL.md` turns that manual A/B-testing loop (which took most of a session) into a repeatable one: look up the target file, generate candidate respellings plus the baseline via the same Azure endpoint, play them, let a human pick (Claude cannot hear audio), write the winning entry. New `frontend/tests/phrase-audio.spec.ts` (speaker button/lock/pill/autoplay across the list page and session stages, RU/EN × desktop/375px screenshots in `temp_files/screenshots/plan_43_phrase-audio/`) and backend cases in `backend/tests/test_audio.py` for real `Phrase`/`CustomPhrase` text, unrecognized text, and the raised length cap. See `plans/improvements/active/plan_43_phrase-audio.md` and `documentation/audio.md`. |
| 44 | 2026-09-23 | TAK on `/dashboard/lists` now says a random easy word instead of the fixed «Sveikas!»: `labas = привет` (RU) / `labas = hello` (EN). New public `GET /api/words/random-easy` returns one random non-archived `star == 1` word (`ORDER BY random() LIMIT 1`, ~2.9k candidates in prod, not cached since it's meant to change per load). Deliberately a separate endpoint, not a field on `/me/stats`: StatsBar refetches stats on every `visibilitychange`, so the word would flip each time the tab regains focus, and `/me/stats` is query-tuned (#15). `StatsBar.tsx` fetches it once on mount, falls back to «Sveikas!» while loading / on error, and caps the mascot at `max-w-[200px]` so long entries (up to 61 chars, e.g. verbs stored with three forms) wrap instead of stretching the card. Other pages keep «Sveikas!». Known data issue, out of scope: some star-1 rows have Russian text in `translation_en`, so EN users may occasionally see a Russian translation. Tests: `backend/tests/test_random_easy_word.py`, `frontend/tests/mascot-random-word.spec.ts` (RU/EN text, error fallback, RU/EN × desktop/375px × short/long word screenshots in `temp_files/screenshots/plan_44_mascot-random-word/`). |
| 45 | 2026-09-23 | Fix #44: the lists-page mascot showed Russian translations to EN users (e.g. `Parama = поддержка`). Root cause: `GET /api/words/random-easy` picked from **every** star-1 `Word`, including words from users' private lists — 629 of 2,908 candidates, all in private lists (`is_public = false`, e.g. «Конституция Глава 2», «From Lessons»), carry the Russian translation copied into `translation_en`. That was also a privacy leak: one user's personal words shown to everyone. Now the endpoint only picks words that sit in at least one public, non-archived list (`EXISTS` subquery, same "never leak private/personal words" rule as the study-session distractors in `words.py`). Pool: 2,051 words, 0 with Cyrillic in `translation_en`; 100/100 live samples clean. #44 was never pushed, so production never exposed it. The underlying data (user-created words storing RU in the EN field) is untouched. `backend/tests/test_random_easy_word.py` now also covers private-list, archived-list and unlisted words. |
